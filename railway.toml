# ─────────────────────────────────────────────────────────────────────────────
# Railway service config — WEB (Next.js app + REST API + IPN webhook)
#
# Point the *web* service at this file:
#   Railway → service → Settings → Config-as-code → Config File Path
#   = /railway.toml
#
# The realtime + bot runtime is a SEPARATE service using Dockerfile.worker and
# `railway.worker.toml`. Do not merge them: the bot takes a Redis singleton lock
# and must be exactly one replica, while the web tier can scale freely.
# ─────────────────────────────────────────────────────────────────────────────

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Migrations run BEFORE the server accepts traffic, so a deploy can never serve
# a new build against an old schema. `migrate deploy` is idempotent and takes
# ~1s when there is nothing to apply, so paying it on every boot is cheap
# insurance. The Prisma CLI is present in this image (see Dockerfile, runner
# stage) which is what makes this possible without a separate migrate service.
#
# TWO GATES, on purpose (2026-09-24 incident): a config-as-code file is a
# PLATFORM setting — if Railway does not pick this file up (Config File Path
# unset, Railpack instead of the Dockerfile, a service created before the file
# existed), the old single-gate arrangement silently ran `npm start` against an
# empty database and served a 500 on every page. So:
#   1. `preDeployCommand` below runs before the new container is promoted, and a
#      failure FAILS the deployment instead of shipping it;
#   2. the image entrypoint (docker-entrypoint.sh) migrates on boot regardless of
#      what Railway is configured to do — that one cannot be skipped.
preDeployCommand = ["npx prisma migrate deploy"]
startCommand = "npm start"

# Railway waits for this to return 2xx before switching traffic to the new
# deployment. This deliberately points at the LIVENESS endpoint, not at the
# dependency-hardened readiness endpoint.
#
# WHY `/healthz` AND NOT `/api/v1/health` (2026-09-26):
#   `/api/v1/health` answers 503 whenever Redis is unreachable. Redis is
#   valuable but it is NOT required for the web tier to serve pages — making a
#   Redis blip fail the platform's own healthcheck pulled every web replica out
#   of rotation (and could fail an otherwise-good deploy), turning a degraded
#   dependency into a total outage.
#
#   `/healthz` answers 200 for any process that is serving HTTP, and reports
#   db/schema/redis/trading in its BODY. A genuinely un-migrated or unreachable
#   database still fails the deploy earlier and harder: the image entrypoint runs
#   `prisma migrate deploy` and exits non-zero (docker-entrypoint.sh), which
#   fails the deployment before this probe is even consulted.
#
#   Keep `/api/v1/health` as the READINESS probe for external monitors (Uptime,
#   Kubernetes) — use it when you want a hard 503 on a degraded dependency.
healthcheckPath = "/healthz"
healthcheckTimeout = 120

restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
